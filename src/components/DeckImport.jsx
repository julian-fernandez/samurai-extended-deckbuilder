// This component is deprecated — import functionality is now handled inline
// in DeckEditor via ImportModal. Kept as a no-op for any remaining references.
const DeckImport = () => null;
export default DeckImport;
